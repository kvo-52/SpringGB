
spring:
  datasource:
    url: jdbc:postgresql://db:5432/postgres?currentSchema=postgresTask1
    username: root
    password: secret
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:liquibase/db.changelog.yaml
    enabled: true
  springdoc:
    override-with-generic-response: false