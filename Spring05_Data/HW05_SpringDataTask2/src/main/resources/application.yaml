spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=postgresTask2
    username: root
    password: secret
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: classpath:liquibase/db.changelog.yaml
    enabled: true
  springdoc:
    override-with-generic-response: false
